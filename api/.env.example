# Copia a api/.env o crea .env en la raíz del monorepo (AZENDA/.env).
# Si existen ambos, api/.env tiene prioridad en claves repetidas.

# Neon (obligatorio):
# La línea NO puede ir comentada con #.
DATABASE_URL=postgresql://USER:PASSWORD@ep-xxxx.region.aws.neon.tech/neondb?sslmode=require

# Opcional:
# DB_BOOTSTRAP_ON_START=1
