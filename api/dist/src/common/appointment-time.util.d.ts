export declare function parseAppointmentWhenLocal(when: string): Date | null;
export declare function appointmentStartMs(when: string): number | null;
export declare function isAppointmentFuture(when: string, nowMs?: number): boolean;
